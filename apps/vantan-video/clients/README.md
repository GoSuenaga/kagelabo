# VANTAN クライアント資産（ローカル）

`apps/vantan-video` のスクリプトは、このディレクトリをカレントから `clients/vantan/...` として参照する。

- `vantan/se/` — 効果音
- `vantan/bgm/` — BGM
- `vantan/2026/` など — ロゴ・スクール別素材

モノレポ全体のクライアント運用はリポジトリルートの `clients/README.md` を参照。
